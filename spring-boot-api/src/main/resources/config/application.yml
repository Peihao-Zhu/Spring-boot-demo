spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/book?useUnicode=true&characterEncoding=utf-8
    username: root
    password: zhupeihao123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

#debug: true

logging:
  level:
    root: info
